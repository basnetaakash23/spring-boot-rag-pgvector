spring:
  ai:
    openai:                    # Embedding model configuration (OpenAI example)
      api-key: ${OPENAI_API_KEY}
      embedding:
        options:
          model: text-embedding-3-large
      chat:
        options:
          model: gpt-4


